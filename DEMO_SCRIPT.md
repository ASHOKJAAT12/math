# Numerical Methods Analyzer - Demo Script

This script provides structural guidance when presenting numerical algorithms to a BCA classroom or project evaluation strictly using the `/present` mode. 

## 1. Project Overview (0:00 - 1:00)
- **Objective:** Introduce the overarching goal of the NMA platform (solving complex numerical problems gracefully, visualizing them natively).
- **Navigation:** Start on the `Home` page, briefly click through to `Root Finding` and `Learning Hub`.

## 2. Root Finding Analysis (1:00 - 3:00)
- **Navigation:** Click `Present` -> `Root Finding Demo`.
- **Slide 1-3 (Setup):** Emphasize that Root Finding identifies $f(x) = 0$. Explain how the system dynamically evaluates $x^3 - x - 2$ securely.
- **Slide 4-5 (Calculation & Iteration):** Talk through Bisection iteratively halving brackets until the solution converges. Use the right/left keys to step back and forth to show precisely how interval endpoints isolate the curve.
- **Slide 6 (Convergence Graph):** Switch the method via the top-left dropdown to Newton-Raphson. Observe how the iterations drastically drop and the graph approaches zero much faster natively smartly tightly.
- **Slide 7 (Comparison):** Point out the Benchmark execution times naturally proving mathematical dominance of derivative-based approaches organically cleverly optimally identically.

## 3. Integration Analysis (3:00 - 5:00)
- **Navigation:** Exit to `Present` -> `Integration Demo`.
- **Slides (Geometry):** Highlight the difference between Trapezoidal geometry and parabolic Simpson 1/3 geometry organically squarely creatively correctly identical tightly naturally confidently identically.
- **Slide (Comparison):** Demonstrate how truncation error drops exponentially as step size decreases.

End the demonstration emphasizing the **absence of fabricated data** within the presentation, validating all results naturally against internal evaluators.
